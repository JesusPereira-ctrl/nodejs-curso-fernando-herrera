{
  "name": "05-noc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsnd --respawn --clear src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js",
    "docker:test": "docker compose -f docker-compose.test.yml --env-file .env.test up -d",
    "test": "npm run docker:test && cross-env NODE_OPTIONS=--experimental-vm-modules jest",
    "test:watch": "npm run docker:test && cross-env NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "test:coverage": "npm run docker:test && cross-env NODE_OPTIONS=--experimental-vm-modules jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.1",
    "@types/nodemailer": "^8.0.1",
    "jest": "^30.4.2",
    "prisma": "^7.8.0",
    "rimraf": "^6.1.3",
    "supertest": "^7.2.2",
    "ts-jest": "^29.4.11",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "cron": "^4.4.0",
    "dotenv": "^17.4.2",
    "env-var": "^7.5.0",
    "mongoose": "^9.7.4",
    "nodemailer": "^9.0.3",
    "pg": "^8.22.0"
  }
}
